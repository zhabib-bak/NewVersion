PORT=3000
NODE_ENV=development
DATA_DIR=./data
SESSION_DURATION_HOURS=12
REMEMBER_ME_DAYS=30
TICKET_APP_DEFAULT_PASSWORD=ChangeMe!2026
# Set to "false" in production to skip forced password reset on seed accounts
# (useful when the DB resets on restart and you have a fixed password configured)
SEED_FORCE_RESET=true
WEBHOOK_TIMEOUT_MS=5000
WEBHOOK_ALLOWLIST=

# PostgreSQL Database Configuration (MySQL or PostgreSQL)
DB_TYPE=postgres
DB_HOST=db.qjocqliwkzflrgcrwxwt.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASS=tweakDenseicky%0

# Email notifications (optional — leave SMTP_HOST empty to disable)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=notifications@example.com
SMTP_PASS=yourpassword
SMTP_FROM=Ticket Tracker <notifications@example.com>
SMTP_SECURE=false
SMTP_REJECT_UNAUTHORIZED=true
